# Research & Design Decisions

## Summary
- **Feature**: `task-detail`
- **Discovery Scope**: New Feature（新規画面）
- **Key Findings**:
  - claude design による画面確定を4ラウンド実施し、レイアウト・インライン編集方式・カテゴライズ・語彙まで確定した
  - レビュー過程で、要件に未反映だった決定（メモ→詳細の改名）と、モックにのみ存在し要件化されていなかった機能（複製、タイムライン絞り込み、期限超過表示、案件解除時の必須タスク自動オフ）が見つかり、`requirements.md` に追加反映した
  - 期限超過の判定基準は、当初案（完了日時の有無）だと中止タスクが誤って超過表示され続ける不整合があり、`task-status-model` のクローズ判定に揃えて訂正した

## ビジュアルデザイン確定（claude design 連携）

**状態: 確定（第4ラウンドで全論点が決着）。** プロンプトは `claude-design-prompt.md`（第1〜第4ラウンド）。

- プロジェクト: 「モック設計レビュー」
- URL: https://claude.ai/design/p/074765e6-99c4-46a5-9c89-66b3ce9d8430?file=Task+Detail+Page.dc.html
- 採用モック: `Task Detail Page.dc.html`

### 確定した視覚仕様（実装はこれに従う）

| 対象 | 確定内容 |
|---|---|
| ページ構成 | 1カラム（上: フィールド、下: タイムライン）。2カラム案（フィールド／タイムライン横並び）は不採用 |
| フィールドカードのカテゴライズ | 単一カード内で「状態」（ステータス・優先度・開発段階）列と「担当・日程・案件」列の2カラムに分け、詳細・親子タスクをその下に続ける。ユーザー自身が claude design 上で直接調整した構成 |
| インライン編集 | ページ全体の「編集」ボタンは廃止。デスクトップは行ホバーで✎がフェードイン（既定 `opacity:0`）。タッチ／狭幅は行タップで選択 → 選択行にのみ✎ボタンを表示 → ✎タップで編集ピッカーを開く（2段階方式、A案） |
| フィールド名 | 「メモ」→「詳細」、「予定日」→「終了予定日」に改称。詳細はPRの説明文相当（背景・経緯を書く欄） |
| 完了日時 | 常に編集不可・✎を出さない。開発段階の変更で自動記録 |
| ステータス | 開発段階が完了種別・中止種別のとき、行・バッジごと非表示（✎も出ない） |
| 必須タスク | 独立した行を持たず、案件名の横に「必須」チップとして表示。編集は案件ピッカーに統合（案件セレクト＋必須トグルを1つのピッカーにまとめる） |
| 期限超過 | 終了予定日が本日より前、かつクローズ済みでない場合に「超過」バッジを表示。判定は完了日時の有無ではなくクローズ済みか（開発段階の種別）で行う（後述の訂正を参照） |
| タイムライン | 新しい順（最新が上）。コメント＝枠線付きカード＋14px本文、フィールド変更＝カードなし1行＋丸マーカー＋13px slate-600。「すべて／コメント／変更履歴」の絞り込みタブを持つ。side-stripe（左の太い色線）は不使用 |
| 削除済み状態 | 完全な参照専用。✎・削除・複製・コメント投稿・復元をすべて非表示。タイムラインに削除イベント自体は表示しない（記録はする） |
| モーダルからの導線 | ヘッダー右上の「詳細ページを開く ↗」リンク（案A）。フッターへの混在（案B）は不採用（保存/キャンセルの決定領域に離脱導線を混ぜない） |

### 主要な設計判断と根拠

- **インライン編集をA案に決定**: 狭幅で✎を常時表示する案は、フィールドが9〜10個並ぶと結局ノイズになる（ユーザー指摘）。B案（✎廃止・値をボタン風に）はフォームに見えて走査性が落ち誤タップリスクも高い。C案（カード単位トグル）はトグル後に✎が一斉表示され同じ問題が再発する。A案（行選択の2段階）は1タップ目が可逆（ハイライトのみ）で誤操作に強く、デスクトップのホバーと同じ「1行＝1ピッカー」の心的モデルを維持できる
- **フィールドカードのカテゴライズ**: 第1ラウンドは「基本情報／案件関連付け／関連タスク」でカードが分かれていたが、統合の過程でフラットな3列グリッドになり単調になった。小見出しでの区分を依頼したところ、最終的にはユーザー自身が2カラム構成に直接調整した
- **メモ→詳細**: 実際の入力内容がPRの説明文（背景・経緯）に近く、「メモ」という名前では位置づけが伝わらないため改称。**API／DB も `detail` に改名する**（実装は `task-field-rename`。完了済み仕様文書は触らない）
- **予定日→終了予定日**: カレンダー等、他画面でも同じフィールドが使われており、入力時に「何の予定日か」が曖昧だったため改称。**API／DB は `scheduledEndDate`、将来の開始予定日は `scheduledStartDate`**（改名実装は `task-field-rename`）
- **必須タスクフラグを独立行にしない**: 案件と必須タスクは1つの業務概念（この案件の完了条件に含めるか）であり、別々の行・別々の✎にすると「編集できない表現」と「編集できる表現」が並ぶ重複が生じていた。1つのピッカーに統合して解消

### 期限超過バッジの判定基準に関する訂正

ユーザーの当初案は「完了日時があるかどうか」で超過を判定するものだった。しかし `task-status-model` の定義では、**中止**種別の開発段階に移されたタスクは完了日時を記録しない（Requirement 2.3）。「完了日時の有無」だけで判定すると、終了予定日を過ぎた中止済みタスクが永久に「超過」と表示され続ける不整合が生じる。

カレンダー画面の期限超過判定（task-status-model Requirement 8.4 / 8.5）は既に「クローズ済みなら期限超過として表示しない」「判定はステータスに基づかない」で統一されているため、詳細ページの超過バッジもこれに揃え、**クローズ済みかどうか（開発段階の種別）**で判定する方針に訂正した。実装コストは同等（開発段階の種別を見るだけ）。`requirements.md` Requirement 1.6 / 1.7 に反映済み。

### レビューで判明した要件の抜け（要件化済み）

claude design のラウンドを重ねる過程で、`requirements.md` に反映されていなかった以下の項目が見つかり、追加した。

| 項目 | 内容 | 反映先 |
|---|---|---|
| メモ→詳細の改名 | 第2ラウンドで決定していたが requirements.md への反映が漏れていた | Requirement 1.1 / 2.1 / 5.1 / 6.2 / 6.4 / 6.6 |
| 複製機能 | モックには一貫してヘッダーに「複製」ボタンが存在していたが、要件に定義がなかった | Requirement 2.9〜2.11、Boundary Context |
| タイムライン絞り込み | 「すべて／コメント／変更履歴」タブがモックに一貫して存在していたが、要件に定義がなかった | Requirement 6.8、Boundary Context |
| 案件解除時の必須タスク自動オフ | モックの注記にのみ存在した派生ルール | Requirement 2.8 |
| 期限超過バッジ | ユーザー要望で追加、上記の通り判定基準を訂正のうえ要件化 | Requirement 1.6 / 1.7、Boundary Context |

### ラウンド履歴（要約）

1. **第1ラウンド**: 初回プロンプト。表示/編集モード、削除済み状態、コメント欄、タイムライン、モーダル縮小の6画面を作成。レイアウトは1a（2カラム）/1b（1カラム）の比較、モーダル導線は案A/Bの比較を依頼
2. **第2ラウンド**: 1bを採用、メモ→詳細に改名、編集UXを全体フォーム切替からインライン編集へ刷新、削除済み状態からタイムラインの削除イベント表示と復元ボタンを撤去、予定日と完了日時の前後関係バリデーション（誤り）を撤去
3. **第3ラウンド**: ✎の視認性を修正（デスクトップはホバー時のみ）。狭幅/タッチの編集アフォーダンスをA/B/C 3案で比較しA案を採用。タイトル直下のバッジとフィールドグリッドの重複を解消
4. **第4ラウンド**: フィールドカードのカテゴライズ依頼（ユーザーが最終的に2カラム構成へ直接調整）。並行してユーザーが「終了予定日」への改称と「超過」バッジを追加

## Risks & Mitigations
- ~~期限超過バッジの実装にはタスクAPIが開発段階の種別（通常/完了/中止）をレスポンスに含めている必要がある~~ → gap分析で確認済み。`DevelopmentStage.kind` が既に存在しAPIレスポンスに含まれる（`development-stage.types.ts`）。API拡張不要
- 「終了予定日」「詳細」への改名および `scheduledDate`→`scheduledEndDate`・`memo`→`detail` は `task-field-rename` に切り出し済み。完了済み仕様文書は更新せず、コードと新仕様のみで揃える。将来の開始予定日は `scheduledStartDate`

---

# Gap Analysis（`/kiro-validate-gap`）

## Analysis Summary
- 表示・軽微編集は既存の `tasks` モジュールとAPI（`PATCH /api/tasks/:id` 等）をほぼそのまま再利用できる。新規なのはページ・インライン編集UI・検索付きセレクトという「見せ方」の部分
- コメント・操作ログはドメインとして完全新規（既存に類似モジュールなし）。特に操作ログは「タスク詳細ページ・詳細モーダル・カンバンボードのどこから変更されても記録する」（Requirement 5.4）という横断要件があり、`TasksService` への一元的なフック設計が肝になる
- 複製機能はバックエンド新規APIなしでも実現可能（既存の作成APIをクライアント側で合成呼び出し）。ただし業務ロジックの一貫性を重視するなら専用エンドポイントの方が安全
- フィールド名は `task-field-rename` により `detail`（詳細）・`scheduledEndDate`（終了予定日）へ揃える前提。追記（2026-08-12）: `task-field-rename` は main にマージ済みで、コード上の語彙も完了している。ギャップ分析時点の「まだ `memo` / `scheduledDate`」は陳腐化した
- 全体の実装規模は L（1〜2週間）。うち操作ログのフック設計とタイムライン集約が最もリスクが高い

## Requirement-to-Asset Map

実装完了後の対応状況（2026-08-12 更新）。

| Requirement | 既存資産 | 実装状況 |
|---|---|---|
| 1. 表示とアクセス | `GET /api/tasks/:id`、`requireWorkspaceMember` ガード、`StatusBadge`/`PriorityBadge`/`StageBadge` | Implemented: `workspaces/[workspaceId]/tasks/[taskId].vue`、終了予定日超過表示、削除済み参照専用 |
| 2. 編集 | `PATCH /api/tasks/:id`（parentTaskId/scheduledEndDate 含む）、status／development-stage | Implemented: InlineEditableField、ParentTaskCombobox、複製（クライアント合成） |
| 3. 削除 | `DELETE /api/tasks/:id`（ソフトデリート） | Implemented: 詳細ページ削除 UI。コメント・操作ログは保持 |
| 4. コメント | `comments` モジュール | Implemented: CRUD、本人チェック、空本文拒否、削除済みタスクへの 409 |
| 5. 操作ログ | `activity-logs` モジュール | Implemented: 追記専用記録、TasksService／CommentService フック、システム操作者 |
| 6. タイムライン | `GET /api/tasks/:id/timeline`、`TaskTimeline.vue` | Implemented: サーバー側 filter、カーソル、表示名解決（候補一覧） |
| 7. モーダル役割分担 | `TaskDetailModal.vue` | Implemented: 簡易表示＋ステータス以外の軽微編集、詳細導線、削除済み参照専用 |

## Implementation Approach Options

### Option A: 既存 `tasks` モジュールを拡張 + 新規 `comments`/`activity-logs` モジュールを追加（採用・実装済み）
- 表示・編集・削除・複製は `tasks` モジュールの既存パターン（routes → service → repository）を拡張
- コメント・操作ログは feature-first の慣習通り、それぞれ独立した新規モジュールとして追加
- 操作ログの記録は `activity-logs` の公開インターフェース経由で集約
- 記録漏れリスクは統合テストで担保

### Option B: 操作ログを Prisma Middleware / Client Extension で横断的に自動記録
- 不採用（業務的分類をフック側で判断するのが困難なため）

### Option C: ハイブリッド（Option A + 契約／統合テスト）
- Option A に統合テストを組み合わせる形で採用

## Effort & Risk

実装時見積もり（参考）。実装は完了済み。

| 領域 | Effort | Risk |
|---|---|---|
| 詳細ページ・表示/軽微編集の拡張 | M | Low |
| インライン編集ピッカー（✎方式） | M | Medium |
| 親タスク検索付きセレクト | S〜M | Medium |
| 複製機能 | S | Low |
| コメントモジュール | S〜M | Low |
| 操作ログモジュール＋既存サービスへのフック | L | Medium〜High |
| タイムライン集約エンドポイント | S〜M | Low |
| モーダル縮小 | S | Low |

**総合**: Effort L、Risk Medium → 実装完了

## Research Needed（design.md で解消済み）

1. ~~カンバンボードのステータス/開発段階変更が `TasksService` の同じメソッドを呼んでいるか~~ → 解消済み
2. ~~繰り返しテンプレートによる自動生成タスクの作成経路~~ → 解消済み
3. ~~`POST /api/tasks/:id/children` と `POST /api/tasks/:id/split` の操作ログ~~ → 解消済み
4. ~~複製APIの実装方式~~ → 解消済み（クライアント合成）
5. ~~タイムラインのページネーション方式~~ → 解消済み（サーバー側 filter + カーソル）

## Next Steps
- 実装・機能検証は完了（`spec.json` phase: `implementation-complete`）
- 乖離是正（2026-08-12）: 要件 7.1 を設計どおり簡易表示に揃え、モーダルの `deletedAt` 参照専用化、非所属取得は 404 に文書統一
