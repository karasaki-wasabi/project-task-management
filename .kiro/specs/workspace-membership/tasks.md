# Implementation Plan

## Implementation Notes

- ビジュアル正本: `research.md`「8. ビジュアルデザイン確定（claude design連携）」の`Workspace Membership.dc.html`
- 実装着手の前提条件は満た済み: `user-auth`は`implementation-complete`で、`requireUser`・CORS/CSRF/セッション配線・ヘッダー（表示名・ログアウト）・`backend/src/test/auth.fixture.ts`・E2E共有 fixture が実コードに存在する。バックエンドAPI公開（本仕様タスク4.1, 4.2）以降はこの前提に乗る
- 統合テストのcurrentUser注入は、既存のセッションCookie付与／CSRFヘッダ付与ヘルパー（`withSessionCookie` / `withCsrfToken`）を再利用する（本仕様では新設しない）
- CSRF・credentials送信は既存`useApiClient`の仕組みにそのまま乗る。本仕様のフロントエンドタスクで新設しない
- 既存データへの影響なし（新規テーブルのみ追加、既存モデルはリレーション配列の追加のみ）
- `deletedAt`は本コードベース共通のsoft-delete拡張を機能させるため両モデルに必須（design.md参照）
- `addMember`で存在しない`userId`を渡すと Prisma FK（P2003）がそのまま上がりうる。API契約の404へ寄せるならルート／サービスで明示マッピングする（タスク4.2で検討）

- [ ] 1. Foundation: スキーマ・共有エラー・型定義
- [x] 1.1 Workspace／WorkspaceMemberのデータモデルを追加してマイグレーションする
  - `Workspace`（名前・識別色・作成者）と`WorkspaceMember`（`(workspaceId, userId)`一意な所属関係）を追加する
  - `User`へ`createdWorkspaces`／`workspaceMemberships`の逆リレーションを追加する（`User`自身のカラムは変更しない）
  - 観測可能な完了状態: マイグレーション適用後、DBに`workspaces`／`workspace_members`テーブルが存在し、同一`(workspace_id, user_id)`の重複挿入がDB制約で拒否される
  - _Requirements: 1.1, 3.1, 4.3_

- [x] 1.2 非メンバー・非作成者操作を拒否するための403エラーヘルパーを追加する
  - `shared/http-errors.ts`に`forbidden`ファクトリを追加する
  - 観測可能な完了状態: `forbidden(...)`を呼ぶとstatusCode 403の`HttpError`が返る単体テストがgreen
  - _Requirements: 3.2, 4.5, 6.5, 7.2, 7.3_

- [x] 1.3 ワークスペースの型定義を用意する
  - `Workspace`／`WorkspaceUserSummary`型と、識別色の固定選択肢（6色）を定義する
  - 観測可能な完了状態: 型と固定選択肢の定数が他モジュールからコンパイルエラーなくimportできる
  - _Requirements: 1.1, 6.3, 6.4_

- [ ] 2. Core: バックエンド — ワークスペースの永続化とユーザー検索
- [x] 2.1 (P) ワークスペースの永続化層を実装する
  - 作成・設定更新・削除（メンバーシップの連鎖削除を含む）・所属ワークスペース一覧・メンバー一覧・所属判定のクエリを実装する
  - 観測可能な完了状態: 各クエリの統合テストが実DBに対してgreen（削除後は一覧・メンバー一覧に現れないことを含む）
  - _Requirements: 1.1, 2.4, 3.1, 6.1, 6.3, 7.1, 7.4_
  - _Boundary: WorkspaceService_

- [x] 2.2 (P) 登録済みユーザーの表示名／メールアドレス検索を追加する
  - 大文字小文字を区別しない部分一致検索を追加し、既存の一覧取得へ任意のクエリパラメータとして統合する（クエリなしの既存呼び出しは従来どおり全件を返す）
  - 観測可能な完了状態: クエリなしで全件、クエリありで名前またはメールが部分一致する利用者のみを返す統合テストがgreen
  - _Requirements: 4.1_
  - _Boundary: UsersSearchExtension_

- [ ] 3. Core: バックエンド — ワークスペースの業務ロジック
- [x] 3.1 ワークスペースの作成・設定更新・削除の業務ルールを実装する
  - 名前は空白のみ不可、識別色は固定選択肢のみ許可、削除は作成者限定、設定更新はメンバー全員可という非対称を実装する
  - 作成時に作成者を同一トランザクションでメンバー登録する
  - 業務イベントログ（作成・更新・削除）を記録する
  - 観測可能な完了状態: 各ルール違反時の拒否（空名・不正な色・非作成者による削除・非メンバーによる設定変更）と正常系が単体テストでgreen
  - _Requirements: 1.1, 1.2, 5.1, 5.2, 6.1, 6.2, 6.3, 6.4, 6.5, 7.1, 7.2, 7.3_
  - _Depends: 2.1_
  - _Boundary: WorkspaceService_

- [x] 3.2 メンバー一覧閲覧・検索追加の業務ロジックを実装する
  - 非メンバーによる閲覧・検索・追加操作を拒否し、既存メンバーを検索結果から除外し、追加時の重複を防ぐ
  - メンバー追加の業務イベントログを記録する
  - 観測可能な完了状態: 既存メンバーが検索結果から除外されること、同一ユーザーの重複追加が拒否されることを単体テストで確認できる
  - _Requirements: 3.1, 3.2, 4.2, 4.3, 4.4, 4.5_
  - _Depends: 2.1, 2.2_
  - _Boundary: WorkspaceService_

- [ ] 4. Integration: ワークスペースAPIの公開
- [x] 4.1 ワークスペース本体のCRUDエンドポイントを公開しアプリへ登録する
  - 作成・一覧・設定更新・削除のエンドポイントをZod検証付きで実装し、`app.ts`へ登録する
  - 各エンドポイントは`user-auth`が適用する認証ガードを通過済みの利用者として処理する（本タスク自身は認証ガードを実装しない。前提条件はImplementation Notes参照）
  - 観測可能な完了状態: 作成・一覧・更新・削除の各エンドポイントがinjectテストで契約どおりのステータス・ボディを返す（正常系・400・403・404を含む）
  - _Requirements: 1.1, 1.2, 2.4, 6.1, 6.2, 6.3, 6.4, 6.5, 7.1, 7.2, 7.3, 7.4_
  - _Depends: 3.1_
  - _Boundary: WorkspaceService_

- [x] 4.2 メンバー関連エンドポイントを公開する
  - メンバー一覧・ユーザー検索・メンバー追加のエンドポイントをZod検証付きで実装し、`app.ts`へ登録する
  - 4.1と同じ認証ガード前提に従う
  - 観測可能な完了状態: メンバー一覧・検索・追加の各エンドポイントがinjectテストで契約どおりのステータス・ボディを返す（正常系・400・403・404を含む）
  - _Requirements: 3.1, 3.2, 4.1, 4.2, 4.3, 4.4, 4.5_
  - _Depends: 3.2_
  - _Boundary: WorkspaceService_

- [ ] 5. Core: フロントエンド — 現在ワークスペースの状態管理
- [x] 5.1 所属ワークスペース一覧の取得と現在選択の保持・切替を実装する
  - ワークスペース一覧取得、選択IDの`localStorage`永続化、所属一覧に含まれないIDの拒否、所属0件時の空状態判定を行う
  - 観測可能な完了状態: 選択したワークスペースIDがページ再読み込み後も保持され、所属一覧に無いIDは無視される
  - _Requirements: 2.1, 2.2, 2.3, 2.4_
  - _Depends: 4.1, 4.2_
  - _Boundary: useCurrentWorkspace_

- [ ] 6. Core: フロントエンド — 画面
- [x] 6.1 ワークスペース作成モーダルを実装する
  - 名前入力・未入力時のエラー表示・作成成功時に新規ワークスペースを現在ワークスペースとして選択する
  - ヘッダー切替（6.2）とメンバー管理画面の空状態（6.3）の両方から呼び出せる形にする
  - 観測可能な完了状態: モーダルから作成すると、作成したワークスペースが即座に現在ワークスペースとして選択された状態になる
  - _Requirements: 1.1, 1.2, 1.3_
  - _Depends: 5.1_
  - _Boundary: WorkspaceCreateModal_

- [x] 6.2 ヘッダーへワークスペース切替を追加する
  - 現在ワークスペース表示・切替ドロップダウン・作成モーダルへの導線・メンバー管理画面への導線・未選択時の淡色表示を実装する
  - 観測可能な完了状態: ブラウザでヘッダーから別ワークスペースへ切り替えると、表示とページ内容が即座に更新される
  - _Requirements: 1.3, 2.1, 2.2, 2.3_
  - _Depends: 5.1_
  - _Boundary: WorkspaceSwitcher, app.vue, app.helpers.ts_

- [x] 6.3 メンバー管理画面の空状態と一覧表示を実装する
  - 現在ワークスペースが無い場合は作成モーダルへの導線を含む空状態カードを、ある場合はメンバー一覧（表示名・メールアドレス）を表示する
  - 観測可能な完了状態: ワークスペース0件時に空状態が、選択中はメンバー一覧が表示される
  - _Requirements: 2.3, 3.1, 3.2_
  - _Depends: 5.1_
  - _Boundary: pages/workspaces_

- [x] 6.4 メンバー検索追加のインラインパネルを実装する
  - 検索欄・結果リスト（既存メンバー除外済み）・0件表示・行内の追加操作を実装する
  - 観測可能な完了状態: 検索して追加すると一覧に即時反映され、再度同じ条件で検索するとその相手が結果から除外されている
  - _Requirements: 4.1, 4.2, 4.3, 4.4, 4.5_
  - _Depends: 5.1_
  - _Boundary: pages/workspaces_

- [x] 6.5 ワークスペース設定モーダル（名前・識別色編集）を実装する
  - 名前入力・固定6色パレットからの選択・保存・キャンセルを実装する
  - メンバー管理画面に設定モーダルを開くトリガー（「設定」操作）を追加する
  - 観測可能な完了状態: メンバー管理画面から設定を開いて保存すると、ヘッダーとメンバー管理画面の見出しの名前・色が更新される
  - _Requirements: 6.1, 6.2, 6.3, 6.4, 6.5_
  - _Depends: 6.3_
  - _Boundary: WorkspaceSettingsModal, pages/workspaces_

- [x] 6.6 作成者限定の削除操作と確認ダイアログを実装する
  - 作成者アカウントにのみ削除ボタンを表示し、確認モーダルでの承認後に削除を実行、削除対象が現在ワークスペースだった場合は選択を解除する
  - 観測可能な完了状態: 作成者以外のアカウントでは削除ボタンが表示されず、作成者は確認モーダルを経て削除でき、削除後は空状態に戻る
  - _Requirements: 7.1, 7.2, 7.3, 7.4_
  - _Depends: 5.1_
  - _Boundary: pages/workspaces_

- [ ] 7. Validation: 統合・E2Eテスト
- [x] 7.1 ワークスペーススコープAPIの権限テストを追加する
  - 非メンバー・非作成者による各エンドポイント呼び出しの拒否（403/404）を横断的に検証する
  - 観測可能な完了状態: 全ワークスペーススコープエンドポイントに対する権限違反系の統合テストがgreen
  - _Requirements: 2.4, 3.2, 4.5, 6.5, 7.2, 7.3_
  - _Depends: 4.1, 4.2_

- [x] 7.2 ワークスペースのクリティカルパスE2Eを追加する
  - ワークスペース0件→作成→ヘッダーでの切替→メンバー検索追加→設定変更→作成者による削除、という一連の操作をE2Eで検証する
  - 観測可能な完了状態: 上記シナリオのE2Eがgreen
  - _Requirements: 1.1, 1.2, 1.3, 2.1, 2.2, 2.3, 6.1, 6.3, 7.1, 7.4_
  - _Depends: 6.4, 6.5, 6.6_
