# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

開発者自身、または少人数チーム。自分たちが担当する案件と、それに付随するタスクの進捗を、PC上で日常的に確認・更新する。リモート/オフィス問わず、個人のPCブラウザからのアクセスを想定。メールアドレス・表示名・パスワードで自己登録し、HttpOnly Cookie セッションでログインして利用する。

## Product Purpose

案件とタスクを一元管理し、「いつまでに何を終わらせる必要があるか」を追跡できるようにする。個人の学習(仕様駆動開発・CI/CD・AWSの段階的実践)を目的として開発しているが、実運用に耐える品質を目指している。

## Positioning

案件単位とタスク単位、両方の粒度の進捗を同時に追える点。多くのタスク管理ツールはタスク単位、多くの案件管理ツールは案件単位のどちらかに寄っているが、このツールは両方を同一画面/同一データモデルで結びつけている(案件の必須タスクの進捗算出、案件終了日超過時の未完了タスク明示など)。

## Operating Context

- ローカル開発はDocker Compose(mysql/backend/frontendの3サービス)。本番はAWS Stage 1(S3+CloudFront+App Runner)を当面のゴールとする(費用を抑えるため、チーム利用が見えるまでStage 2/3は保留)。
- 主な操作フロー: タスクの作成・開発段階の移動（完了種別到達で完了日時を打刻）・段階内ステータス変更(未着手/作業中/引継待ち/保留)・階層化・分割、案件の登録と必須タスク進捗確認、カレンダーでのタスク期限日・案件期間の俯瞰、案件連動の繰り返しタスクテンプレート設定と非営業日ポリシー適用、休日マスタ整備、期間別消化数ダッシュボード、担当者によるタスク一覧の絞り込み。
- 画面構成: ワークスペース選択後にダッシュボード、タスク、カンバン、案件、カレンダー、繰り返し設定、消化数、ユーザー、休日マスタなど（URL は `/workspaces/:workspaceId/...`）。

## Capabilities and Constraints

- タスクの親子階層・分割、開発段階による完了／クローズ判定、段階内ステータス、案件の必須タスク進捗算出、カレンダー表示(タスク期限日・案件期間)、繰り返しタスク生成(案件連動のみ × 起点4種 × 非営業日ポリシー4種、作成/編集時の確認付き適用)、休日マスタ、消化数集計とフォーキャスト、担当者フィルタ、ワークスペース分離、運用ログ基盤(アクセス/業務イベント/エラー/フロントエンドエラーをrequestId相関)。
- 認証はアカウント登録と Cookie セッションまで。RBAC・招待・外部 IdP・機械用トークンは対象外。
- 通知・リマインダー配信、JIRA等外部システム連携は対象外(意図的な制約)。
- 非営業日(祝日)の取得は常に手動トリガーで、自動ポーリング・スケジュール同期は行わない。
- フロントエンドはNuxt 4(Vue3, `ssr: false`の静的SPA)。ブラウザから直接バックエンドAPIを呼ぶ構成のため、CORS/環境変数配線が必須(詳細は`.kiro/steering/local-dev-pitfalls.md`)。

## Brand Commitments

未確立。プロダクト名・ロゴ・トンマナは特に定めていない(社内ツールとしての実用性を優先)。

## Evidence on Hand

実データ・実績値は現時点でなし。デモ用のシードデータやテストデータ(`e2e-`プレフィックス等)のみ存在。今後のデザイン作業で実在の顧客事例・数値実績を捏造しないこと。

## Product Principles

- 認証は登録とセッションに留め、RBAC・通知・外部連携などの重い機能を持たない、責務の狭いツールであり続ける(各仕様の Boundary Commitments に準拠)。
- 案件単位とタスク単位、両方の粒度の進捗を常に同時に把握できる状態を保つ。
- バックエンドが返すエラー(業務ルール違反等)は、コンソールに埋もれさせず必ず画面上に表示する(`.kiro/steering/error-handling.md`のパターンを踏襲)。
- 低頻度・少人数アクセスを前提とした、実用最優先のUI(過度な演出よりも状態の把握しやすさを優先)。

## Accessibility & Inclusion

現時点で確立された要件なし。
